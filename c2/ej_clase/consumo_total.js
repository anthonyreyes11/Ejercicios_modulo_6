//crear un scrip que llame dos subprocesos , uno recibe  el num de kilometros 
// y el otro que recibe el consumo.
// calcular  cuanto $ vale el viaje

// conmentamos esto

/*const child_process =require('child_process')

child_process.exec('node kilometros.js',function (err,resultado1) {
  const kilometros = Number(resultado1)

  child_process.exec('node consumo.js', function (err,resultado2) {
    const consumo = Number(resultado2)

    const litros_consumidos = kilometros/consumo
    console.log (`el gasto total $${litros_consumidos*1240}`)
    
  })
})*/

